[pytest]
minversion = 7.0
addopts = -ra -q
python_files = test_*.py
python_classes = Test*
python_functions = test_
# Restrict test discovery to the gsim tests directory
testpaths = tests

# Ensure parent directory (project root) is on sys.path so `import gsim` works
pythonpath = ..
